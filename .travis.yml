language: rust
rust:
  - nightly
cache: cargo

before_script:
  - export PATH="$PATH:$HOME/.cargo/bin"
  - which rustfmt || (cd .. && cargo install rustfmt)

script:
  - cargo build
  - cargo fmt -- --write-mode=diff
  - cargo test
